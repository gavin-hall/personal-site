export default function PostTitle({ children }) {
  return (
    <h1 className="font-heading mb-12 text-center text-4xl leading-tight tracking-tight md:text-5xl md:leading-none lg:text-6xl">
      {children}
    </h1>
  )
}
